menu "DMA drivers"

config DMA_GD32VF103
       depends on RISCV
       bool "GD32VF103 series DMA"

config DMA_RP2040
       depends on ARM
       bool "Raspberry RP2040 DMA"

config DMA_ATSAME5X
       depends on ARM
       bool "ATSAME5x/D5x series DMA"

config DMA_STM32H7XX
       depends on ARM
       bool "STM32H7xx series DMA"

config DMA_TI_F28X
       depends on C2000
       bool "TI TMS320F28x series DMA"

config DMA_NXP_EDMA
       depends on POWERPC
       bool "NXP eDMA DMA"

endmenu
